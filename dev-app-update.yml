provider: github
# These are only used if you force-enable the updater in dev.
# For packaged builds, electron-builder generates the update config from electron-builder.yml.
owner: YOUR_GITHUB_OWNER
repo: YOUR_GITHUB_REPO
updaterCacheDirName: arc-db-browser-updater
